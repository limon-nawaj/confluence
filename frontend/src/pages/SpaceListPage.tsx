import { useNavigate } from 'react-router-dom'
import DashboardPage from './DashboardPage'

// Space list is shown on the dashboard for simplicity
export default function SpaceListPage() {
  return <DashboardPage />
}
